Bài kiểm tra cuối kỳ có thể sẽ bao gồm các câu hỏi trắc nghiệm để kiểm tra kiến thức của bạn về những nội dung sau:

```
CSP (bài toán vùng tranh chấp - critical section problem)
HTTP
SIGINT
TCP
TLB (bộ nhớ đệm chuyển đổi địa chỉ - Translation Lookaside Buffer)
Bộ nhớ ảo
mảng
rào cản (barrier)
chuỗi C
chmod
máy khách / máy chủ (client/server)
điều kiện Coffman
biến điều kiện (condition variables)
chuyển đổi ngữ cảnh (context switch)
deadlock (bế tắc)
bài toán triết gia ăn tối (dining philosophers)
epoll
exit
nhập/xuất tệp (file I/O)
biểu diễn hệ thống tệp
fork/exec/wait
fprintf
free
bộ cấp phát heap (heap allocator)
heap/stack
inode so với tên
malloc
mkfifo
mmap
mutex
cổng mạng (network ports)
mở/đóng (open/close)
thuật ngữ hệ điều hành
lỗi trang (page fault)
bảng trang (page tables)
ống (pipes)
số học con trỏ (pointer arithmetic)
con trỏ (pointers)
in (`printf`)
nhà sản xuất/người tiêu dùng (producer/consumer)
tiến trình/mutex
điều kiện cuộc đua (race conditions)
đọc/ghi (read/write)
người đọc/người viết (reader/writer)
biểu đồ phân bổ tài nguyên (resource allocation graphs)
bộ đệm vòng (ring buffer)
`scanf`
bộ đệm (buffering)
lập lịch (scheduling)
select
semaphore
tín hiệu (signals)
`sizeof`
`stat`
`stderr`/`stdout`
liên kết tượng trưng (symlinks)
điều khiển luồng (`_create`, `_join`, `_exit`)
khởi tạo biến (variable initializers)
phạm vi biến (variable scope)
thrashing bộ nhớ ảo (VM thrashing)
macro chờ đợi (wait macros)
ghi/đọc với `errno`, `EINTR` và dữ liệu một phần 
```

